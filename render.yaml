services:
  - type: web
    name: moderation-backend
    env: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r ../requirements.txt     # <-- Note the new relative path
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENAI_API_KEY
        value: placeholder
