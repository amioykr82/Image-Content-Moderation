services:
  - type: web
    name: moderation-backend
    env: python
    plan: free
    rootDir: backend                   # ✅ Tells Render to run from backend/
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENAI_API_KEY
        value: placeholder             # Add the real key in Render dashboard
